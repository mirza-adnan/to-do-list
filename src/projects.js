export default class Project {
  constructor(name) {
    this.name = name;
    this.tasks = [];
  }

  getName() {
    return this.name;
  }

  getTasks() {
    return this.tasks;
  }

  addTask(newTask) {
    this.tasks.forEach(task => {
      if task.name === new 
    })
  }
}